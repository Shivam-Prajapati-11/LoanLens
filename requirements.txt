# Loan Approval Prediction - project dependencies
# Install with:  python -m pip install -r requirements.txt

# --- Machine learning (training + EDA) ---
pandas>=2.0,<3.0
numpy>=1.24,<3.0
scikit-learn>=1.4,<2.0
joblib>=1.3,<2.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0

# --- Backend API (serving) ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.5,<3.0

# --- Tests (fastapi.testclient is built on httpx) ---
httpx>=0.27,<1.0

# --- Database (SQLite via SQLAlchemy) ---
SQLAlchemy>=2.0,<3.0

